<%- include ('../partials/head') %>
<%- include ('../partials/header') %>
<main class="main-main profile-main">
    <section class="profile-section">
        <div class="profile-title">
            <h1>
                TU PERFIL
            </h1>
        </div>
        <div class="profile-name">
            <h2>
                <%= user.name %> <%= user.surname %>
            </h2>
        </div>
        <div class="profile-avatar">
            <img src="/img/users/<%= user.avatar %>">
        </div>
        <div class="profile-more">
            <h3>
                <%= user.email %>
            </h3>
            <h3>
                Argentina
            </h3>
            <h4>
                ¿Estás subscripto a nuestra newsletter?
            </h4>
            <h1>
                <% if (user.newsletter === true) { %>
                    <i class="fas fa-check newsletter-true"></i>
                <% } else { %>
                    <i class="fas fa-times newsletter-false"></i>
                <% } %>
            </h1>
        </div>
    </section>
    <form action="/users/profile/<%= user.id %>?_method=PUT" method="POST">
        <button>
            Editar mis datos
        </button>
    </form>
</main>
<%- include ('../partials/footer') %>