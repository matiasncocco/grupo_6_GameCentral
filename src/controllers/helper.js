let helper = {
    addOne: (n) => {
        n++;
        return n;
    },

    giveNumber: (s) => {
        n = parseInt(s);
        return n;
    },

    times: (n1, n2) => {
        return n1 * n2;
    }
};

module.exports = helper;